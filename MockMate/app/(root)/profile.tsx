import { UniSafeAreaView } from "@/core/customUniwind";
import { Text } from "react-native";

const Profile = () => {
	return (
		<UniSafeAreaView>
			<Text>profile</Text>
		</UniSafeAreaView>
	);
};

export default Profile;
